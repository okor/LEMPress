Need to...
  Create mysql user
  Set password
  
  Create wp-config.php
  Set username
  Set database
  Set prefix
  Set password

  If the script fails, it should be able to be re-run without bonking anything. Currently, It will fail on symlinking. Probably other things.